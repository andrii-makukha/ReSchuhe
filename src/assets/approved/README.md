# Approved Assets

This directory receives immutable, versioned Approved Assets through `pnpm assets:import`.

Do not place drafts, source files, prompts, private rights documents, or manually copied output here.
Every version directory must contain only its sanitized `asset-manifest.json` and the files listed by
that manifest.
